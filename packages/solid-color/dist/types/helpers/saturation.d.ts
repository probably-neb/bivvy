import { HslColor } from '../types';
export declare function calculateChange(e: any, hsl: HslColor, container: HTMLDivElement): {
    h: number;
    s: number;
    v: number;
    a: number;
    source: string;
};
