import { HslColor } from '../types';
export declare const calculateChange: (e: any, hsl: HslColor, direction: string, initialA: any, container: HTMLDivElement) => {
    h: number;
    s: number;
    l: number;
    a: any;
    source: string;
};
