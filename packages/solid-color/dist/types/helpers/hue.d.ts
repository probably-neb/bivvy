import { HslColor } from '../types';
export declare const calculateChange: (e: any, direction: string, hsl: HslColor, container: HTMLDivElement) => {
    h: any;
    s: number;
    l: number;
    a: number;
    source: string;
};
